---
description: Compile a LaTeX paper to PDF and fix build issues.
---
Load and follow the `paper-compile` skill from `.opencode/skills/paper-compile/SKILL.md`.

Use `$ARGUMENTS` as the paper directory or target.
