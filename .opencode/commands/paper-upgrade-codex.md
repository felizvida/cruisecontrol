---
description: Upgrade a linked or local paper using the pure Codex route.
---
Load and follow the `paper-upgrade` skill from `.opencode/skills/paper-upgrade/SKILL.md`.

Use `$ARGUMENTS` as the source paper or upgrade scope.
Force workflow route: `codex`.
Keep all artifacts in the current local repository.
