---
description: Check availability and obtain a moderate one-node GPU allocation on NIH Biowulf, then use modules and scratch correctly on the compute node.
---
Load and follow the `biowulf-gpu` skill from `.opencode/skills/biowulf-gpu/SKILL.md`.

Use `$ARGUMENTS` as the request description.
Read project metadata from the active repo's `AGENTS.md` when present.
