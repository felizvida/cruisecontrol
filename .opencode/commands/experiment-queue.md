---
description: Queue and orchestrate multi-job experiment batches with GPU-aware scheduling and retry logic.
---
Load and follow the `experiment-queue` skill from `.opencode/skills/experiment-queue/SKILL.md`.

Use `$ARGUMENTS` as the manifest path, grid spec, or batch description.
Keep manifests, queue state, and logs in the current local repository unless the user explicitly asks otherwise.
