---
description: Monitor experiments, collect results, and summarize status.
---
Load and follow the `monitor-experiment` skill from `.opencode/skills/monitor-experiment/SKILL.md`.

Use `$ARGUMENTS` as the server alias, session name, or monitoring scope.
