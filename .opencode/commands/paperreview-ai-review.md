---
description: Submit a paper PDF to paperreview.ai and save the returned token, review, and score locally.
---
Load and follow the `paperreview-ai-review` skill from `.opencode/skills/paperreview-ai-review/SKILL.md`.

Use `$ARGUMENTS` as the paper PDF path and save all outputs under the current repo's `review/` directory.
Resolve the submission email from explicit user input, `PAPERREVIEW_EMAIL`, or the project's `AGENTS.md`.
