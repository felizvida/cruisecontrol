---
description: Audit a paper's quantitative claims against the raw result files that support them.
---
Load and follow the `paper-claim-audit` skill from `.opencode/skills/paper-claim-audit/SKILL.md`.

Use `$ARGUMENTS` as the paper directory.
Default workflow route: `codex`. If the user explicitly wants the pure OpenCode route, honor that with `route: opencode`.
Write the audit artifacts into the local paper directory only.
