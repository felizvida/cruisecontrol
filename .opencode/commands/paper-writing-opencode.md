---
description: Run the full paper-writing pipeline using the pure OpenCode route.
---
Load and follow the `paper-writing` skill from `.opencode/skills/paper-writing/SKILL.md`.

Use `$ARGUMENTS` as the narrative report path or paper topic.
Force workflow route: `opencode`.
Write all artifacts to local project files only.
