---
description: Generate a deterministic SVG architecture or workflow figure from structured JSON.
---
Load and follow the `figure-spec` skill from `.opencode/skills/figure-spec/SKILL.md`.

Use `$ARGUMENTS` as the diagram description or spec path.
Keep the spec file and rendered SVG in the current local repository.
