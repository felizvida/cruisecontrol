---
description: Deploy and launch ML experiments on local or remote compute.
---
Load and follow the `run-experiment` skill from `.opencode/skills/run-experiment/SKILL.md`.

Use `$ARGUMENTS` as the experiment description.
Read project metadata from the active repo's `AGENTS.md`.
