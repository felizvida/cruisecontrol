# Serialization View Figure

This directory contains the standalone source and generated high-resolution exports for Figure 1 in the paper.

## Rebuild

From the example's `code/` directory:

```bash
./generate_figure_assets.sh
```
