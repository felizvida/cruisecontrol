# Figure Assets

Each figure subdirectory in this folder contains:

- vector PDF
- vector SVG
- 600 DPI PNG

The authoritative generation path is:

```bash
cd examples/codex-zebrafish-brain-paper-14rounds/code
./build_inputs.sh
```

